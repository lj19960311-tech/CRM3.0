# 销售秘书3.0 项目规则

## 1. 项目根目录

- “销售秘书3.0”所有任务生成、修改、整理和交付的文件，必须直接保存到：`/Users/liujun/Documents/销售秘书3.0`。
- 不得只把正式文件保存在临时目录、缓存目录、Codex/ChatGPT 项目镜像或其他工作目录中。
- 除非用户明确指定其他位置，否则无需再次询问保存路径，直接按本规则分类保存。
- 交付前必须确认正式文件及其配套资源已经完整写入项目根目录。

## 2. 固定目录结构

- `01-Demo/`：可运行的 Demo、HTML 演示、交互原型、预览版本及其配套文件。
- `02-需求文档/`：正式需求文档、Markdown、Word、PDF、方案文档及其配套资源。
- `03-原始资料/`：用户提供或要求归档的原始 Word、PDF、Excel、Markdown、录音转写等输入材料。
- `04-图片资源/`：独立图片、截图、流程图、设计图、视觉素材和导出图片。
- `05-历史版本/`：正式文件修改前的旧版本和阶段性备份。
- 不得在项目根目录重复创建无编号的 `Demo`、`需求文档`、`原始资料`、`图片资源`、`历史版本` 文件夹。

## 3. 自动分类规则

- 演示版本和原型保存到 `01-Demo/`。
- 最终需求文档及其配套资源保存到 `02-需求文档/`。
- 原始输入材料保存到 `03-原始资料/`，并保持原文件不变。
- 独立图片和视觉资源保存到 `04-图片资源/`。
- 修改已有正式文件前，先把旧版本复制到 `05-历史版本/`，文件名增加日期时间，避免覆盖。
- Markdown 文档引用的配套资源放在与文档同级的 `<文档名>_assets/` 文件夹中，并使用相对路径引用。
- 与某个 Demo 强关联的脚本、样式和图片可放在该 Demo 自己的同名资源目录中，保证 Demo 可独立运行。

## 4. 原始资料与历史版本保护

- 原始资料只读使用，不得直接覆盖、改名或删除；需要加工时先复制到相应工作目录。
- 修改正式交付文件前必须备份旧版本；历史版本不得与最新版混放。
- 删除文件或包含内容的文件夹前，必须确认目标明确且属于用户要求删除的范围。
- 不得使用清理操作误删正式交付物、原始资料或历史版本。

## 5. 交付检查

- 检查文件能正常打开，必要的图片、样式、脚本和附件没有缺失。
- 检查 Markdown、HTML 等文件中的相对路径有效。
- 检查最终文件位于正确的编号目录，不依赖临时目录中的文件。
- 向用户提供正式目录中的文件链接或准确路径。
- 若交付包含多个文件，说明主文件和配套资源所在位置。

## 6. Git 自动提交与远程备份

- 项目 Git 仓库根目录为：`/Users/liujun/Documents/销售秘书3.0`。
- 远程仓库为：`https://github.com/lj19960311-tech/CRM3.0.git`。
- 默认分支为：`main`。
- 每次完成文件生成、修改或整理，并通过交付检查后，自动检查本次变更范围。
- 只提交本次任务相关文件以及明确属于项目且尚未备份的正式文件，不提交无关文件。
- 使用简洁中文编写提交说明，准确概括本次交付内容。
- 完成本地提交后自动推送到 `origin/main`，无需再次询问是否提交或推送。
- 推送后核对本地 `main` 与 `origin/main` 是否一致；只有验证成功后，才向用户说明云端备份完成。
- 若网络、认证或权限导致推送失败，必须保留本地提交，明确说明失败原因，并在网络或授权恢复后重试。
- 本地 Git 不能代替异地备份；只有成功推送到远程仓库才算完成云端备份。

## 7. 安全与忽略规则

- 不得把 GitHub 密码、Personal Access Token、验证码、私钥、API Key 或其他敏感凭据写入项目文件或提交到 Git。
- `.DS_Store`、临时文件、交换文件和缓存文件不提交到 Git。
- 不得在聊天中要求用户发送密码或完整访问令牌。
- 发现疑似敏感信息时，提交前停止并排除该文件或内容。

## 8. 默认执行顺序

1. 确认任务产物类型。
2. 在对应编号目录中生成或修改文件。
3. 修改旧文件前备份到 `05-历史版本/`。
4. 检查文件完整性、可打开性和资源引用。
5. 检查 Git 变更范围，排除临时文件与敏感信息。
6. 创建中文 Git 提交。
7. 推送到 `origin/main`。
8. 验证远程同步成功后，再向用户交付文件路径和提交结果。
